{
  "name": "vue3",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm build-integration && vite",
    "build": "run-p type-check build-only",
    "build-integration": "pnpm -C ../../ build",
    "preview": "vite preview --port 4173",
    "build-only": "vite build",
    "type-check": "vue-tsc --noEmit"
  },
  "dependencies": {
    "@sagi.io/globalthis": "^0.0.2",
    "vue": "^3.0.0",
    "vue-i18n-phrase-in-context-editor": "link:../.."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "@vue/tsconfig": "^0.5.0",
    "npm-run-all": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vue-i18n": "^9.0.0",
    "vue-tsc": "^1.0.0"
  }
}
